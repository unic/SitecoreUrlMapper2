trigger:
  branches:
    include:
    - feature/*
    - master
    - develop
    - release/*
    - hotfix/*
  paths:
    include:
    - be/*

jobs:
- template: '/build/azure-templates/jobs-build-be.yml'
